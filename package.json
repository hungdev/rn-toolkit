{
  "private": true,
  "scripts": {
    "start": "cd example && yarn start",
    "build": "cd library && yarn build --watch"
  },
  "workspaces": [
    "library",
    "example"
  ],
  "devDependencies": {
    "expo-yarn-workspaces": "^1.7.0"
  }
}
